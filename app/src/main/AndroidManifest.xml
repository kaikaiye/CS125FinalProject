<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.owner.cs125finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Levels_Menu"></activity>
        <activity android:name=".ThreeDigitLevel"></activity>
        <activity android:name=".FourDigitLevel"></activity>
        <activity android:name=".FiveDigitLevel"></activity>
        <activity android:name=".GameEndLoss"></activity>
        <activity android:name=".GameEndWin"></activity>
        <activity android:name=".Instructions"></activity>
        <activity android:name=".ThreeDigitLevelWithTimer"></activity>
        <activity android:name=".FourDigitLevelWithTimer"></activity>
        <activity android:name=".FiveDigitLevelWithTimer"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>